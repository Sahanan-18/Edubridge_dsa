Pseudocodes 
1.Pseudocode of sum of two numbers.
for i from 0 to n - 1:
    for j from i + 1 to n - 1:
        if arr[i] + arr[j] == target:
            return (i, j)
print "No pair found"


2.Pseudocode of Binary Search.
low = 0
high = n - 1
while low <= high:
    mid = (low + high) / 2
    if arr[mid] == target:
        return mid
    else if arr[mid] < target:
        low = mid + 1
    else:
        high = mid - 1
return -1


3.Pseudocode of Anagram check.
if length(s1) != length(s2):
    return false
initialize count1[256] = {0}, count2[256] = {0}
for each character in s1:
    increment count1[char]
for each character in s2:
    increment count2[char]
for i in 0 to 255:
    if count1[i] != count2[i]:
        return false
return true


4.Pseudocode of  Longest substring.
initialize lastIndex[256] = {-1}
start = 0, maxLen = 0
for i from 0 to n - 1:
    if lastIndex[s[i]] >= start:
        start = lastIndex[s[i]] + 1
    update lastIndex[s[i]] = i
    maxLen = max(maxLen, i - start + 1)
return maxLen


5.Pseudocode of Count of Palindrome Substring.
count = 0
for i from 0 to n - 1:
    count += expandAroundCenter(i, i)    
    count += expandAroundCenter(i, i + 1)
function expandAroundCenter(left, right):
    while left >= 0 and right < n and s[left] == s[right]:
        count++
        left--
        right++
    return count
